using _Scripts.ContainerSystem;

namespace _Scripts._Player
{
    public class PlayerHandContainer : BaseContainer
    {
        public void Initialize(int maxCount)
        {
            base.Initialize(maxCount);
        }
    }
}